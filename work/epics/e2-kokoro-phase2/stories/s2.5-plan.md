# S2.5 Plan — /kokoro-validate Skill

> **Story:** S2.5 | **Epic:** E2 | **Size:** S | **Tasks:** 2

## Task Breakdown

### T1: RED — Failing content validation tests

**Goal:** Write tests that define the contract for the `/kokoro-validate` skill file.
All tests must fail because the skill file does not yet exist.

**Steps:**

1. Create `tests/test_validate.py` with RED tests mirroring `test_forces.py` patterns:
   - `TestValidateSkillExists`
     - `test_skill_file_exists` — `extension/.claude/commands/kokoro-validate.md` exists
   - `TestValidateStructure`
     - `test_has_3x3x3_model` — "3x3x3" or ("3 anos" + "3 meses" + "3 semanas") in content
     - `test_has_risk_categories` — all 3: "riesgo de producto", "riesgo de consumidor" or "riesgo de invitado", "riesgo de mercado"
     - `test_has_experiment_design` — "hipotesis" and "experimento" in content
     - `test_has_validation_plan_canvas` — "validation plan" or "plan de validacion" in lower
   - `TestValidateContent`
     - `test_has_eduardo_voice` — Eduardo vocabulary present (invitado, creacion, inversion — all 3)
     - `test_references_knowledge_file` — references `kokoro-phase2-validation.md`
     - `test_has_anti_pattern_warnings` — warnings about solution love, Excel projections, or measuring intentions
     - `test_has_proyector_strategy` — Proyector strategy guidance (permiso or invitacion)
   - `TestValidateSections`
     - `test_has_traction_concept` — "traccion" in lower
     - `test_has_mindset_principles` — "principio" or "mindset" in lower
     - `test_experiment_section_has_guiding_questions` — experiment section has >= 2 question marks
   - `TestValidateSummary`
     - `test_has_summary_template` — summary section with table template >= 4 rows
   - `TestValidateCLI`
     - `test_init_copies_validate_skill` — `kokoro init` copies file to target `.claude/commands/`
     - `test_copied_skill_matches_source` — copied file matches source exactly

**Verify:**
- `uv run pytest tests/test_validate.py` → FAIL (skill file doesn't exist)
- `uv run pytest tests/test_interviews.py tests/test_forces.py` → PASS (no regressions)
- `uv run pyright` → PASS
- `uv run ruff check` → PASS

**Commit:** `feat(s2.5): T1 — RED failing content validation tests for /kokoro-validate`

---

### T2: GREEN — Create kokoro-validate.md skill file

**Goal:** Create the skill file with Eduardo's voice, 3x3x3 model, risk categories,
experiment design, traction, Validation Plan Canvas, anti-patterns, and summary
template. All T1 tests pass.

**Steps:**

1. Create `extension/.claude/commands/kokoro-validate.md` with:
   - Opening quote: "La velocidad de aprendizaje es la ventaja injusta."
   - `## Contexto` — purpose, experiment-driven validation, build-measure-learn
   - `## Antes de Empezar — Estrategia del Proyector` — ask permission, set context
   - `## Los 10 Principios del Mindset Emprendedor` — summarized principles
   - `## Modelo de Planificacion 3x3x3` — 3 anos, 3 meses, 3 semanas sprints
   - `## Identificacion de Riesgos` — 3 categories (Producto, Consumidor/Invitado, Mercado)
   - `## Diseno de Experimentos` — hypothesis template, qualitative vs quantitative, guiding questions
   - `## Concepto de Traccion` — definition, positive/negative indicators
   - `## Validation Plan Canvas` — Leanstack 6-step process
   - `## Errores Comunes` — anti-pattern warnings
   - `## Resumen` — markdown table template >= 4 rows
   - `## Notas para Claude` — coaching instructions, Eduardo vocabulary
   - Reference to `kokoro-phase2-validation.md` knowledge file

**Verify:**
- `uv run pytest tests/test_validate.py` → PASS (all GREEN)
- `uv run pytest` → PASS (full suite, no regressions)
- `uv run pyright` → PASS
- `uv run ruff check` → PASS

**Commit:** `feat(s2.5): T2 — GREEN kokoro-validate.md skill file with Eduardo's voice`

---

## Execution Order

```
T1 (RED) → T2 (GREEN)
```

Strictly sequential. T2 depends on T1 tests existing to validate against.

REFACTOR is omitted as a separate task — this is Size S and the pattern is proven
from S2.2/S2.3. Any polish happens inline during T2.

## Patterns Applied

- **PAT-L-001** — Skill file TDD pattern (RED → GREEN)
- **PAT-L-006** — Structural TDD for content (tests define markers before files exist)
- **PAT-L-009** — kokoro- prefix convention for managed files
- **PAT-L-014** — String match semantics specified with exact match table in design phase
- **PAT-L-015** — List comprehensions over manual append loops (for pyright compliance)

## Design Decisions Implemented

| ID | Decision | Task |
|----|----------|------|
| D1 | Summarize 10 principles, not full listing | T2 |
| D2 | 3 risk categories as separate subsections | T1, T2 |
| D3 | Hypothesis template as structured format | T2 |
| D4 | Single markdown file following PAT-L-001 | T1, T2 |
| D5 | Test file mirrors test_forces.py structure | T1 |

## Estimated Effort

| Task | Effort |
|------|--------|
| T1   | ~20 min |
| T2   | ~35 min |
| **Total** | **~55 min** |
