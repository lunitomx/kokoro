# S2.1 Plan — Knowledge Files Phase 2

> **Story:** S2.1 | **Epic:** E2 | **Size:** S | **Tasks:** 3

## Task Breakdown

### T1: RED — Failing content validation tests + conftest.py

**Goal:** Write tests that define the contract for Phase 2 knowledge files. Create
shared fixtures in conftest.py to eliminate duplication from E1.

**Steps:**

1. Create `tests/conftest.py` with shared path fixtures:
   - `extension_path` → project root / "extension"
   - `knowledge_path` → extension_path / ".claude" / "knowledge"
   - `commands_path` → extension_path / ".claude" / "commands"
2. Create `tests/test_knowledge_phase2.py` with RED tests:
   - `PHASE2_FILES` list: canvas, forces, interviews, validation
   - `test_file_exists` — parametrized, each of 4 files exists
   - `test_file_has_kokoro_managed_header` — managed header present
   - `test_file_has_proposito_section` — "## Proposito" section exists
   - `test_file_has_conexion_section` — "## Conexion con Otros Skills" section
   - `test_file_has_substantive_content` — >= 30 lines
   - `test_canvas_has_lean_methodology` — keywords: segmento, propuesta unica de valor, ventaja injusta
   - `test_forces_has_customer_forces` — keywords: trigger, inercia, friccion, oferta mafia
   - `test_interviews_has_methodology` — keywords: escuchar, 15 minutos, problema interview
   - `test_validation_has_experiment_design` — keywords: 3x3x3, hipotesis, construir-medir-aprender
3. Update `tests/test_knowledge.py` to use conftest fixtures where applicable
   (replace `KNOWLEDGE_DIR` with `knowledge_path` fixture)

**Verify:**
- `uv run pytest tests/test_knowledge_phase2.py` → FAIL (files don't exist)
- `uv run pytest tests/test_knowledge.py` → PASS (existing tests still work)
- `uv run pyright` → PASS
- `uv run ruff check` → PASS

**Commit:** `feat(s2.1): T1 — RED failing content tests + conftest.py`

---

### T2: GREEN — Create 4 knowledge files

**Goal:** Create the Phase 2 knowledge files with Eduardo's methodology content
sourced from `conocimientoraiz/02_Modulo_Semilla_ModeloExperiencia.md`.

**Steps:**

1. Create `extension/.claude/knowledge/kokoro-phase2-canvas.md`
   - kokoro-managed header
   - Skill: `/kokoro-canvas` | Fase 2: Elegir la Semilla
   - Proposito: guide entrepreneur through Lean Canvas
   - Content: 3 questions, 9 blocks with segment-first order, PUV formula,
     ventaja injusta criteria, risk analysis by type
   - Eduardo vocabulary: inversion (not precio), creacion (not producto)
   - Conexion: links to diagnostico (Phase 1), forces, validation

2. Create `extension/.claude/knowledge/kokoro-phase2-forces.md`
   - kokoro-managed header
   - Skill: `/kokoro-forces` | Fase 2: Elegir la Semilla
   - Proposito: map customer decision forces
   - Content: 4 forces (trigger, push, inercia, pull/friccion), Mafia Offer
     structure, customer criteria, momento aja, best selling moments
   - Conexion: links to canvas, interviews

3. Create `extension/.claude/knowledge/kokoro-phase2-interviews.md`
   - kokoro-managed header
   - Skill: `/kokoro-interviews` | Fase 2: Elegir la Semilla
   - Proposito: guide problem and solution interviews
   - Content: problem interview structure, 15 min active listening, sell
     nothing, pattern analysis across 10 interviews, solution interview,
     behaviors > intentions
   - Conexion: links to forces, validation

4. Create `extension/.claude/knowledge/kokoro-phase2-validation.md`
   - kokoro-managed header
   - Skill: `/kokoro-validation` | Fase 2: Elegir la Semilla
   - Proposito: design validation experiments
   - Content: 3x3x3 model, build-measure-learn cycle, risk identification from
     canvas, riskiest assumption first, qualitative vs quantitative,
     experiment design template
   - Conexion: links to montana del manana (Phase 1), canvas, interviews

**Verify:**
- `uv run pytest tests/test_knowledge_phase2.py` → PASS
- `uv run pytest tests/test_knowledge.py` → PASS
- `uv run pyright` → PASS
- `uv run ruff check` → PASS

**Commit:** `feat(s2.1): T2 — GREEN 4 Phase 2 knowledge files`

---

### T3: Integration verification

**Goal:** Verify knowledge files integrate with kokoro init and full test suite passes.

**Steps:**

1. Verify knowledge files are registered in pyproject.toml package data
   (or kokoro init copies them correctly)
2. Run full test suite: `uv run pytest` → ALL PASS
3. Run type checks: `uv run pyright` → PASS
4. Run linting: `uv run ruff check` → PASS

**Verify:**
- `uv run pytest` → ALL PASS (no regressions)

**Commit:** Only if pyproject.toml or other integration changes needed.

---

## Execution Order

```
T1 (RED) → T2 (GREEN) → T3 (Integration)
```

Strictly sequential. Each task depends on the previous.

## Patterns Applied

- **PAT-L-006** — Structural TDD for content (tests define markers before files exist)
- **PAT-L-008** — Knowledge file template structure
- **PAT-L-009** — kokoro- prefix convention for managed files

## Estimated Effort

| Task | Effort |
|------|--------|
| T1   | ~20 min |
| T2   | ~40 min |
| T3   | ~10 min |
| **Total** | **~70 min** |
