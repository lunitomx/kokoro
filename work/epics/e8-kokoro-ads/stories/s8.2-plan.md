---
story_id: "S8.2"
epic_ref: "E8"
status: "approved"
---

# Plan: S8.2 — Register kokoro-ads in test suite

## Tasks

### Task 1: Add kokoro-ads to test registration lists

**File:** `tests/test_output_structure.py`

1. Add `"kokoro-ads"` to `COACHING_SKILLS` list (after `"kokoro-rhythm"`)
2. Add `"kokoro-ads"` to `VALID_COMMANDS` set
3. Add `"kokoro-ads"` to `EXISTING_COMMANDS` set

### Task 2: Verify all tests pass

Run `uv run pytest tests/ -v` and confirm:
- All existing tests still pass
- New kokoro-ads parametrized tests pass
- Total test count increases by 3+ (new parametrized cases)
