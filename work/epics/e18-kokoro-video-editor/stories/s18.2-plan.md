---
story_id: "S18.2"
epic_id: "E18"
title: "/kokoro-cuts — Plan"
status: "planned"
---

# Plan: S18.2 — /kokoro-cuts

## Tasks

### T1: RED — Register kokoro-cuts in test constants, verify failure

Add "kokoro-cuts" to COACHING_SKILLS, EXISTING_COMMANDS in
`tests/test_output_structure.py`. VALID_COMMANDS already has
"kokoro-cuts" (added as planned during E18 setup). Run tests — they
fail because the skill file does not exist yet.

**Files modified:**
- `tests/test_output_structure.py`

**Verify:** `pytest tests/test_output_structure.py -x -q` fails with
missing file error for kokoro-cuts.

### T2: GREEN — Create skill file, knowledge file, and extension mirrors

Create `.claude/commands/kokoro-cuts.md` with:
- Header: transversal video-editing tool, reads kokoro-listen transcripts
- Input: transcript file path (positional argument)
- Instructions: read transcript, analyze full text for best moments,
  produce 1-10 ranked cuts using sentence-boundary references
- Scoring signals: content density, hook potential, topic transitions,
  emotional peaks, standalone value (pure LLM inference, no heuristics)
- Duration scaling: <5 min → 1-3 cuts, 5-30 min → 3-7 cuts, 30+ min → 5-10
- Output template with: summary table (rank, score, title, duration),
  per-cut detail (hook, excerpt, rationale), siguiente paso
  (/kokoro-shorts, /kokoro-creative)
- JSON output saved alongside transcript (`*-cuts.json`) with: rank,
  score, title, hook, excerpt, start_sentence, end_sentence,
  estimated_duration_seconds, rationale
- Anti-patterns: vocabulary substitutions, no generic tips
- Session log persistence

Create `.claude/knowledge/kokoro-cuts-signals.md` with:
- Cut signal definitions (content density, hook potential, etc.)
- Transcript format documentation (kokoro-listen header + plain text)
- Duration-to-cuts scaling rules
- JSON output schema reference

Copy both files to extension mirrors:
- `extension/.claude/commands/kokoro-cuts.md`
- `extension/.claude/knowledge/kokoro-cuts-signals.md`

**Files created:**
- `.claude/commands/kokoro-cuts.md`
- `.claude/knowledge/kokoro-cuts-signals.md`
- `extension/.claude/commands/kokoro-cuts.md`
- `extension/.claude/knowledge/kokoro-cuts-signals.md`

**Verify:** `pytest tests/ -x -q` passes — all tests green including
new kokoro-cuts registrations (output template, cross-refs, structure).

### T3: Integration — Verify with real transcript

Run `/kokoro-cuts` against the test transcript at
`/tmp/kokoro-listen/3zXGLEgl5L8-chatgpt-reuniones.txt` (3:43 duration).

Verify:
- Produces 1-3 cuts (short video scaling)
- Each cut has: excerpt, start_sentence, end_sentence, title, hook, score, rationale
- JSON file saved at `/tmp/kokoro-listen/3zXGLEgl5L8-chatgpt-reuniones-cuts.json`
- Human-readable output has summary table, per-cut detail, siguiente paso
- No prohibited vocabulary in output

**Note:** This is a manual HITL step — run interactively, review output
with Eduardo.

## Sequence

T1 → T2 → T3 (linear, each depends on previous)

## Commit Strategy

- Commit after T1 (RED state): `test(s18.2): add kokoro-cuts to test constants — RED`
- Commit after T2 (GREEN state): `feat(s18.2): /kokoro-cuts skill + knowledge — GREEN`
- T3 is manual verification, no commit needed
