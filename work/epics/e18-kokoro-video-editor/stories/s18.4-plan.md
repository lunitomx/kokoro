---
story_id: "S18.4"
epic_id: "E18"
title: "Plan — /kokoro-overlay drawtext captions + vertical 9:16 default"
tasks: 4
---

# S18.4 Plan — /kokoro-overlay

## Task Overview

| # | Task | Files | Commit message | Risk |
|---|------|-------|----------------|------|
| T1 | RED — Add kokoro-overlay to EXISTING_COMMANDS | `tests/test_output_structure.py` | `test(s18.4): RED — add kokoro-overlay to EXISTING_COMMANDS` | Low |
| T2 | GREEN — Create skill file + knowledge file | `.claude/commands/kokoro-overlay.md`, `.claude/knowledge/kokoro-overlay-ffmpeg.md` | `feat(s18.4): GREEN — kokoro-overlay skill + ffmpeg drawtext knowledge` | Medium |
| T3 | Update kokoro-shorts pointer + verify | `.claude/commands/kokoro-shorts.md`, `tests/test_output_structure.py` | `chore(s18.4): update kokoro-shorts next-step pointer to kokoro-overlay` | Low |
| T4 | Integration — end-to-end manual validation | (no file changes — manual run) | n/a | Low |

## Dependencies

```
T1 (RED) → T2 (GREEN) → T3 (pointer + verify) → T4 (integration)
```

Linear chain. Each task depends on the previous.

## Task Details

### T1: RED — Add kokoro-overlay to EXISTING_COMMANDS

**Goal:** Failing test that proves the skill file doesn't exist yet.

**Steps:**
1. Read `tests/test_output_structure.py`
2. Add `"kokoro-overlay"` to the `EXISTING_COMMANDS` set (after `"kokoro-shorts"`)
3. Run `uv run pytest tests/test_output_structure.py -x` — expect FAIL
4. Commit (RED state)

**Verification:** Test fails with assertion error — `.claude/commands/kokoro-overlay.md` not found.

**Files modified:**
- `tests/test_output_structure.py` — add entry to `EXISTING_COMMANDS`

---

### T2: GREEN — Create skill file + knowledge file

**Goal:** All tests pass. The skill file and knowledge file exist with complete content.

**Steps:**
1. Create `.claude/commands/kokoro-overlay.md` with full skill instructions:
   - Pipeline stages: transcript extraction, escaping, line wrapping, filter construction, vertical reformat, output
   - Single-mode and batch-mode invocation
   - Default style: fontsize=48, white text, black stroke borderw=3, y=h*0.80
   - Escaping function for drawtext special chars (`:`, `'`, `\`, `%`)
   - Line wrapping at ~35 chars
   - Output naming: `{basename}-captioned.mp4` + summary JSON
   - Error handling for all failure modes from design
   - Invocation examples (single file, batch, keep-original)
2. Create `.claude/knowledge/kokoro-overlay-ffmpeg.md` with drawtext reference:
   - Complete drawtext filter syntax
   - Escaping rules with examples
   - enable/between timing syntax
   - crop+scale+drawtext combined filter chain
   - Common errors and troubleshooting
3. Run `uv run pytest tests/test_output_structure.py -x` — expect PASS
4. Run `uv run pyright` if applicable
5. Commit (GREEN state)

**Verification:** All tests pass. Skill file exists and is well-formed markdown.

**Files created:**
- `.claude/commands/kokoro-overlay.md`
- `.claude/knowledge/kokoro-overlay-ffmpeg.md`

---

### T3: Update kokoro-shorts pointer + verify

**Goal:** kokoro-shorts references kokoro-overlay as the next pipeline step.

**Steps:**
1. Read `.claude/commands/kokoro-shorts.md`
2. Add a "Next Step" or "Pipeline" section pointing to `/kokoro-overlay` for adding captions
3. Run full test suite to verify nothing broke
4. Commit

**Verification:** kokoro-shorts.md contains reference to `/kokoro-overlay`. Tests pass.

**Files modified:**
- `.claude/commands/kokoro-shorts.md` — add next-step pointer

---

### T4: Integration — end-to-end manual validation

**Goal:** Confirm the skill produces correct ffmpeg commands with real data.

**Steps:**
1. Use existing test video and Whisper JSON from S18.3 work (in `/tmp/kokoro-shorts/`)
2. Invoke `/kokoro-overlay` with the test short and transcript
3. Verify:
   - AC1: Captions synchronized within 0.5s of Whisper timestamps
   - AC2: Output is vertical 9:16 (1080x1920)
   - AC3: White text with black stroke border (readable)
   - AC4: Output named `{basename}-captioned.mp4` with summary JSON
   - AC5: Special characters escaped properly in ffmpeg command
4. If S18.3 test files are not available, construct a minimal test scenario with sample Whisper JSON

**Verification:** Manual — video plays correctly with captions. HITL review.

**Files modified:** None (manual validation).

## Acceptance Criteria Traceability

| AC | Covered by |
|----|------------|
| AC1: Captions synchronized within 0.5s | T2 (skill uses Whisper timestamps directly), T4 (manual verification) |
| AC2: Default vertical 9:16, overridable | T2 (skill file documents default + override), T4 (manual verification) |
| AC3: Readable captions (white + black stroke) | T2 (style spec in skill file), T4 (visual check) |
| AC4: Output as {basename}-captioned.mp4 + JSON | T2 (output spec in skill file), T4 (file existence check) |
| AC5: Special chars escaped for drawtext | T2 (escaping function in skill + knowledge file), T4 (test with special chars) |

## Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| ffmpeg drawtext escaping edge cases | Knowledge file documents all metacharacters; escaping function is explicit |
| Line wrapping inaccurate for variable-width fonts | ~35 char threshold is conservative; visual check in T4 |
| S18.3 test files may not be on disk | T4 can construct minimal sample data |
| Font availability across systems | Design specifies no external fonts; ffmpeg default font is sufficient |
