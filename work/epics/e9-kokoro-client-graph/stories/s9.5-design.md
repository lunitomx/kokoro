---
story_id: "S9.5"
status: "designed"
---

# S9.5 Design — Memory Sync

## Decisions

- **D1:** New file `src/kokoro/clients/sync.py` with sync_to_memory function
- **D2:** Generate reference_*.md with YAML frontmatter (name, description, type: reference)
- **D3:** Don't overwrite existing files unless force=True
- **D4:** Slug derived from client name: lowercase, spaces/hyphens to underscores

## Components

- **CREATE:** `src/kokoro/clients/sync.py` — generate_reference_md, sync_to_memory
- **CREATE:** `tests/test_client_sync.py` — all test scenarios
- **MODIFY:** `src/kokoro/clients/__init__.py` — add sync exports

## Interface

```python
def generate_reference_md(client: ClientProfile) -> str: ...
def sync_to_memory(registry: ClientRegistry, memory_dir: Path, *, force: bool = False) -> list[Path]: ...
```
