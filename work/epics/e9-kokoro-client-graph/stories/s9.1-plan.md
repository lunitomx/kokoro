---
story_id: "S9.1"
epic_ref: "E9"
---

# S9.1 Plan — Client Data Models

## Tasks

| # | Task | Phase | Commit |
|---|------|-------|--------|
| 1 | Create `tests/test_client_models.py` with all test cases | RED | `test(s9.1): RED — client model tests before implementation` |
| 2 | Create `src/kokoro/clients/__init__.py` and `models.py` | GREEN | `feat(s9.1): GREEN — ClientProfile and ClientRegistry models` |
| 3 | Run pyright + ruff + full test suite, fix if needed | REFACTOR | Only if fixes needed |

## Task Details

### Task 1: Tests (RED)

Create `tests/test_client_models.py` with:
- `TestClientProfile`: test_create_minimal, test_create_full, test_missing_required_field
- `TestClientRegistry`: fixture with 2 clients, test_find_by_id, test_find_by_id_not_found, test_find_by_name, test_find_by_name_case_insensitive, test_find_by_name_not_found, test_find_by_segment, test_find_by_segment_none, test_list_groups, test_empty_registry

**Total: 11 test cases**

Verification: `uv run pytest tests/test_client_models.py` should FAIL (ImportError).

### Task 2: Implementation (GREEN)

Create module with two files:
- `src/kokoro/clients/__init__.py` — docstring + public exports
- `src/kokoro/clients/models.py` — ClientProfile, ClientRegistry

Verification: `uv run pytest tests/test_client_models.py` should PASS (11/11).

### Task 3: Validation (REFACTOR)

Run quality gates:
- `uv run pyright src/kokoro/clients/`
- `uv run ruff check src/kokoro/clients/`
- `uv run pytest tests/ --tb=short` (full suite)

Fix any issues. Commit only if changes were needed.

## Dependencies

None — this is the foundation story.
