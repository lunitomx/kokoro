# S3.2: Persistence Layer — Scope

> **Status:** In Progress

## In Scope

- `src/kokoro/ontology/store.py` — JSON persistence module
- `load_state(project_dir)` → CoachingState | None
- `save_state(project_dir, state)` → None (atomic write)
- `create_empty_state(negocio)` → CoachingState
- `mark_skill_complete(state, skill, summary, nodes)` → CoachingState
- `get_skill_context(state, skill)` → relevant nodes for that skill
- Atomic write: write to .tmp, rename
- Unit tests for all functions

## Out of Scope

- Skill markdown changes (S3.3/S3.4)
- CLI command for state query
- Session/router integration (S3.5)

## Done Criteria

- [ ] load_state returns None when no file exists
- [ ] save_state + load_state round-trips without data loss
- [ ] Atomic write via temp file + rename
- [ ] mark_skill_complete updates phase progress correctly
- [ ] get_skill_context returns relevant nodes per skill
- [ ] Pyright strict: 0 errors
- [ ] Ruff: 0 errors
- [ ] All 336+ tests pass
