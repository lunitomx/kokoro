---
story_id: "S32.3"
epic_id: "E32"
title: "Plan: Tests + verificacion"
task_count: 3
estimated_size: "S"
---

# S32.3 — Story Plan: Tests + verificacion

## Task Breakdown

### Task 1 (S): Run 5 verification checks

**Goal:** Execute all 5 verification checks against `extension/.claude/commands/kokoro-landing.md`.

**Steps:**

1. **Vocabulary compliance** — Grep for forbidden words (precio, producto, gratis,
   descuento, cliente, comprar, barato, vender, problema, gastar) in the command
   file. Exclude the vocabulary reference table and anti-patrones section where
   these words appear as "don't say" examples. Any occurrence in instructional
   content = FAIL.

2. **Output structure** — Read the command file and verify the structured output
   template contains all 4 required sections:
   - Mapa de Bloques table
   - Scorecard de Principios table
   - Diagnostico por Bloque section
   - Top 3 Acciones section
   Missing any section = FAIL.

3. **Knowledge accuracy** — Read `extension/.claude/knowledge/kokoro/kokoro-lean-landing.md`
   and cross-reference against the command file:
   - All 9 blocks present with correct names and order (01 HERO through 09 CTA PERSISTENTE)
   - All 5 principles present with correct names
   Any mismatch in name, count, or order = FAIL.

4. **Integration** — Verify `/kokoro-audit` is mentioned as a complement (suggested
   at end of diagnosis, not required as a prerequisite). If `/kokoro-audit` is
   positioned as a dependency or prerequisite = FAIL.

5. **Mirror consistency** — Diff `extension/.claude/commands/kokoro-landing.md`
   against `.claude/commands/kokoro-landing.md`. Any difference = FAIL.

**Files:** (read-only)
- `extension/.claude/commands/kokoro-landing.md`
- `.claude/commands/kokoro-landing.md`
- `extension/.claude/knowledge/kokoro/kokoro-lean-landing.md`

**Verify:** All 5 checks produce a clear PASS or FAIL result with evidence.

**Commit:** No — results feed into Task 2.

---

### Task 2 (XS): Create verification report

**Goal:** Document verification results as a structured report artifact.

**Steps:**

1. Create `work/epics/e32-kokoro-landing/stories/s32.3-verification-report.md`
2. Write YAML frontmatter (story_id, date, status)
3. For each of the 5 checks, document:
   - Check name
   - PASS/FAIL status
   - Evidence (what was found or confirmed)
   - Notes (if any issues or observations)
4. Write summary section with overall verdict

**Files:** `work/epics/e32-kokoro-landing/stories/s32.3-verification-report.md`

**Verify:** Report contains all 5 checks with explicit PASS/FAIL per check.

**Commit:** Yes — `feat(s32.3): verification report — 5 checks documented`

---

### Task 3 (XS): Verify skill registration

**Goal:** Confirm `/kokoro-landing` appears in the available skills list.

**Steps:**

1. Check that `kokoro-landing` appears in the skill list (visible in the
   system-reminder skills listing)
2. Verify the skill description matches: "Auditoria Estrategica de Landing Pages"
3. Document result in the verification report (append or note in Task 2)

**Files:** None (observation only)

**Verify:** `kokoro-landing` is listed and accessible as a skill.

**Commit:** No — observation only, captured in Task 2 report.

---

## Execution Order

```
T1 (S) → T2 (XS) → T3 (XS)
```

Linear. T1 produces the raw check results. T2 formalizes them as a report.
T3 confirms skill visibility (can run in parallel with T2 but sequenced for
simplicity).

## Estimated Effort

| Task | Size | Description |
|------|------|-------------|
| T1 | S | Run 5 verification checks |
| T2 | XS | Create verification report |
| T3 | XS | Verify skill registration |
| **Total** | **S** | 3 tasks, linear |
