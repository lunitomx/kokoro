---
story_id: "S11.1"
epic_id: "E11"
title: "Plan — Register MCP Servers in Claude Code"
tasks: 2
complexity: "XS"
---

# S11.1 Plan — Register MCP Servers

## Tasks

### Task 1: Register MCP servers

**Action:**
1. Run `claude mcp add facebook-ads -- uv --directory /Users/soyahuehuetedigital/Documents/MétricaRadix/mcpfbads run src/server.py`
2. Run `claude mcp add google-ads -- uv --directory /Users/soyahuehuetedigital/Documents/MétricaRadix/mcpgoogleads run src/server.py`

**Verification:**
- Both entries appear in Claude Code MCP config (`claude mcp list`)
- Server names match convention: `facebook-ads`, `google-ads`

**Depends on:** nothing

---

### Task 2: Integration test — verify all 4 MCP servers accessible

**Action:**
1. List MCP tools to confirm `facebook-ads` and `google-ads` tools appear
2. Verify `google-analytics` and `google-search-console` still work
3. Document results in commit message

**Verification:**
- All 4 servers listed: google-analytics, google-search-console, facebook-ads, google-ads
- No regressions on existing servers

**Depends on:** Task 1

---

## Execution Order

```
Task 1 (register) → Task 2 (verify)
```

## Notes

- Config-only story — no Python code, no unit tests
- Verification is the "test": servers registered and tools listed
- No env vars in Claude Code config (servers load `.env` internally)
