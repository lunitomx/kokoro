---
story_id: "S11.5"
epic_id: "E11"
title: "/kokoro-scorecard — Plan"
status: "planned"
---

# Plan: S11.5 — /kokoro-scorecard

## Tasks

### T1: RED — Write failing tests for kokoro-scorecard registration

Add "kokoro-scorecard" to COACHING_SKILLS, VALID_COMMANDS, and
EXISTING_COMMANDS in test_output_structure.py. Run tests — they fail
because the skill file does not exist yet.

**Verify**: `pytest tests/test_output_structure.py -x` fails with
missing file error.

### T2: GREEN — Create kokoro-scorecard.md skill file

Create `.claude/commands/kokoro-scorecard.md` with:
- Header: transversal tool, depends on /kokoro-connect
- Context: client resolution pattern (same as analytics)
- Instructions: query all platforms, compare periods, synthesize
- Output template with: header, per-platform summary, health indicators,
  cross-platform insights, recommended actions, siguiente paso
- Anti-patterns: vocabulary, process, content
- Notas para Claude
- Session log persistence

**Verify**: skill file passes vocabulary check (no prohibited words in
non-exempt contexts).

### T3: GREEN — Create extension mirror

Copy `.claude/commands/kokoro-scorecard.md` to
`extension/.claude/commands/kokoro-scorecard.md`.

**Verify**: files are identical.

### T4: GREEN — Run full test suite

Run `pytest` to verify all tests pass including new registrations.

**Verify**: all tests pass, zero failures.

### T5: Commit implementation

Stage and commit all changes.

## Sequence

T1 → T2 → T3 → T4 → T5 (linear, each depends on previous)
