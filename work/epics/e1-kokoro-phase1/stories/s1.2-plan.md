# S1.2 Plan: CLAUDE.md Brain

> **Story:** S1.2 | **Size:** L | **Date:** 2026-03-02

## Notes on TDD

This is a content story — the deliverable is a .md file, not traditional code.
TDD adapts as: write structural tests first (required sections, keywords, anti-patterns),
then write the content to satisfy them. This ensures completeness without testing prose quality.

## Tasks

### T1: Structural tests for CLAUDE.md (S)

**Description:** Write tests that define what a complete CLAUDE.md must contain: required sections, vocabulary rules, anti-pattern keywords absent, bilingual markers.

**TDD Cycle:**
1. **RED:** Write tests asserting CLAUDE.md has required sections and keywords
2. Tests fail because CLAUDE.md is placeholder

**Files:**
- `tests/test_claude_md.py` — structural validation tests

**AC Reference:** All scenarios in s1.2-story.md

**Verification:**
```bash
pytest tests/test_claude_md.py -v  # should FAIL (RED)
```

**Dependencies:** None

### T2: Identity core + Projector strategy (M)

**Description:** Write the opening sections of CLAUDE.md — who Eduardo is, his philosophy, Projector HD strategy (wait for invitation), and the fundamental tone.

**Sources:** libro-summary.md (arquetipos, HD Proyector, Eneagrama 3w4)

**TDD Cycle:**
1. **GREEN:** Write identity sections to pass T1 identity tests

**Files:**
- `extension/.claude/CLAUDE.md` — replace placeholder

**Verification:**
```bash
pytest tests/test_claude_md.py -k "identity" -v
```

**Dependencies:** T1

### T3: Voice patterns + vocabulary (M)

**Description:** Add Eduardo's voice rules — luxurizante vocabulary table, interaction patterns (espejo antes que consejo, 70/30, amortiguar-pivotar-ofrecer), sprezzatura, metaphor density.

**Sources:** libro-summary.md (voz y estilo), luxelling-summary.md (módulo 05, 06)

**TDD Cycle:**
1. **GREEN:** Write voice sections to pass T1 vocabulary/voice tests

**Files:**
- `extension/.claude/CLAUDE.md` — add voice sections

**Verification:**
```bash
pytest tests/test_claude_md.py -k "voice or vocabulary" -v
```

**Dependencies:** T2

### T4: Methodology + skill routing (S)

**Description:** Add the 4-phase methodology overview and hints about available /kokoro-* skills. Eduardo's process from diagnostic to harvest.

**Sources:** kokoro-project.md (4 phases, skill list)

**TDD Cycle:**
1. **GREEN:** Write methodology sections to pass T1 methodology tests

**Files:**
- `extension/.claude/CLAUDE.md` — add methodology sections

**Verification:**
```bash
pytest tests/test_claude_md.py -k "methodology" -v
```

**Dependencies:** T2

### T5: Anti-patterns + bilingual instructions (S)

**Description:** Add what Eduardo would NEVER say, generic AI patterns to avoid, and bilingual response rules (Spanish soul, respond in user's language).

**Sources:** libro-summary.md (anti-patterns), s1.2-story.md (examples table)

**TDD Cycle:**
1. **GREEN:** Write anti-pattern and bilingual sections to pass remaining T1 tests

**Files:**
- `extension/.claude/CLAUDE.md` — add anti-patterns and bilingual sections

**Verification:**
```bash
pytest tests/test_claude_md.py -v  # ALL should pass now
ruff check tests/
```

**Dependencies:** T2

### T6: Integration test (XS)

**Description:** Run `kokoro init` with the real CLAUDE.md, verify it installs correctly and the marker merge works with the full content.

**Steps:**
1. Reinstall package (`uv pip install -e .`)
2. `kokoro init` in temp dir — verify full CLAUDE.md present
3. `kokoro init` in dir with existing CLAUDE.md — verify merge
4. All existing tests still pass

**Verification:**
```bash
pytest tests/ -v  # ALL tests pass
```

**Dependencies:** T1-T5

## Execution Order

```
T1 (structural tests) → T2 (identity) → T3 (voice) ─┐
                                        → T4 (methodology) ─┤ → T5 (anti-patterns) → T6 (integration)
                                                             ┘
```

T3 and T4 can be written in any order after T2, but are sequential in practice (single developer). T5 closes remaining test gaps. T6 validates end-to-end.

**Rationale:** Risk-first — T2 (identity/voice core) is the hardest and riskiest. If Eduardo's voice doesn't land, we pivot early.

## Risks

| Risk | L/I | Mitigation |
|------|:---:|------------|
| Eduardo's voice too generic | M/H | Use specific phrases from libro-summary, not paraphrases |
| CLAUDE.md too long for context | L/M | Keep concise — Claude Code loads full file |
| Structural tests too rigid for prose | M/L | Test sections and keywords, not exact wording |

## Duration Tracking

| Task | Planned | Actual | Status |
|------|:-------:|:------:|:------:|
| T1 | S | S | Done (SES-002) |
| T2 | M | S | Done (SES-003) |
| T3 | M | S | Done (SES-003) |
| T4 | S | S | Done (SES-003) |
| T5 | S | S | Done (SES-003) |
| T6 | XS | XS | Done (SES-003) |
