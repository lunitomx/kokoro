# S1.1 Scope: Package Skeleton

## In Scope
- pyproject.toml with build configuration, entry points, package data
- src/kokoro/__init__.py with version
- src/kokoro/cli.py with `kokoro init` command
- extension/.claude/ directory structure (empty placeholder files)
- Tests for CLI init behavior (fresh + existing .claude/)

## Out of Scope
- CLAUDE.md content (S1.2)
- Knowledge file content (S1.3)
- Skill .md content (S1.4–S1.8)
- Publishing to PyPI (manual GitHub install for now)

## Done Criteria
- `pip install -e .` succeeds from repo root
- `kokoro init` creates .claude/ with correct structure
- `kokoro init` handles existing .claude/CLAUDE.md with markers
- Tests pass for fresh install and existing project scenarios
- pyright + ruff pass
