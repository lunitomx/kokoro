# S1.1 Plan: Package Skeleton

> **Story:** S1.1 | **Size:** XS | **Date:** 2026-03-02

## Tasks

### T1: Package structure + kokoro init CLI (S)

**Description:** Create the full package structure and implement `kokoro init` with TDD.

**TDD Cycle:**
1. **RED:** Write tests for `kokoro init` — fresh directory, existing .claude/, marker append
2. **GREEN:** Implement pyproject.toml, `__init__.py`, `cli.py`, extension placeholders
3. **REFACTOR:** Clean up, ensure type annotations pass pyright

**Files to create:**
- `pyproject.toml` — build config, entry point `kokoro = "kokoro.cli:main"`, package data
- `src/kokoro/__init__.py` — version string
- `src/kokoro/cli.py` — `init()` function + `main()` CLI entry point
- `extension/.claude/CLAUDE.md` — placeholder (content in S1.2)
- `extension/.claude/commands/.gitkeep` — placeholder (skills in S1.4–S1.8)
- `extension/.claude/knowledge/.gitkeep` — placeholder (content in S1.3)
- `tests/__init__.py`
- `tests/test_cli.py` — test fresh init, existing .claude/, marker logic

**AC Reference:** All three Gherkin scenarios in s1.1-story.md

**Verification:**
```bash
pytest tests/ -v
ruff check src/ tests/
pyright src/
```

**Dependencies:** None

### T2: Integration test (XS)

**Description:** Manual end-to-end validation — pip install and kokoro init in a temp directory.

**Steps:**
1. `pip install -e .` from repo root
2. `kokoro` command is available
3. `kokoro init` in a temp directory creates .claude/ structure
4. `kokoro init` in a directory with existing .claude/CLAUDE.md appends with markers
5. Verify all expected files exist

**Verification:** Manual pass/fail

**Dependencies:** T1

## Execution Order

```
T1 (package + CLI + tests) → T2 (integration test)
```

**Rationale:** Single implementation pass (XS story), then validate end-to-end.

## Risks

| Risk | L/I | Mitigation |
|------|:---:|------------|
| package_data not found at runtime | M/M | Test with `pip install -e .` early in T1 |

## Duration Tracking

| Task | Planned | Actual | Status |
|------|:-------:|:------:|:------:|
| T1 | S | S | Done |
| T2 | XS | XS | Done |
