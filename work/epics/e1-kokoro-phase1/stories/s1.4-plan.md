# S1.4 Plan: /kokoro-diagnose Skill

**Size:** M (3 tasks)
**Date:** 2026-03-07
**Design:** s1.4-design.md

## Tasks

### T1: RED -- Write failing content validation tests (S)

**TDD Cycle:**
- RED: Create `tests/test_diagnose.py` with tests that validate skill file existence and content
- Tests must fail because `kokoro-diagnose.md` does not yet exist

**Tests:**
- `test_skill_file_exists` -- skill file at `extension/.claude/commands/kokoro-diagnose.md`
- `test_skill_has_speed_boat_section` -- contains Speed Boat guidance (anchors, wind, rocks)
- `test_skill_has_vision_2020_section` -- contains Vision 20/20 guidance (clear, blurry, blind spots)
- `test_skill_has_diagnostic_summary` -- contains structured summary section
- `test_skill_has_eduardo_voice` -- contains Projector strategy markers, Eduardo's vocabulary
- `test_cli_copies_diagnose_skill` -- `kokoro init` copies skill to target `.claude/commands/`

**Files:**
- `tests/test_diagnose.py` -- create

**Verification:** `uv run pytest tests/test_diagnose.py` -- all FAIL (RED)

**AC refs:** AC1-AC6

### T2: GREEN -- Create skill file with Eduardo's voice (M)

**TDD Cycle:**
- GREEN: Create `extension/.claude/commands/kokoro-diagnose.md` with full content
- Skill file structure: instructions, Speed Boat exercise, Vision 20/20 exercise, diagnostic summary
- Eduardo's voice: Projector strategy (ask before guiding), metaphors, vocabulary
- Reference to `kokoro-phase1-diagnostico.md` knowledge file

**Files:**
- `extension/.claude/commands/kokoro-diagnose.md` -- create

**Verification:** `uv run pytest tests/test_diagnose.py` -- all PASS (GREEN)

**AC refs:** AC1-AC4

**Dependencies:** T1

### T3: Integration test -- kokoro init copies skill (S)

**TDD Cycle:**
- Verify existing `kokoro init` copies the new skill file alongside other commands
- Run full test suite to confirm no regressions
- Add specific diagnose copy assertion to CLI integration test if not already covered by T1

**Files:**
- `tests/test_diagnose.py` -- possibly extend with integration test

**Verification:** `uv run pytest --tb=short && uv run pyright && uv run ruff check .`

**AC refs:** AC5, AC6

**Dependencies:** T2

## Execution Order

1. **T1** -- Tests first (TDD RED). Defines the contract for the skill file content.
2. **T2** -- Skill file creation (TDD GREEN). Bulk of creative work. Depends on T1 to know what passes.
3. **T3** -- Integration validation. Confirms CLI copies the new file and full suite is clean.

**Rationale:** Standard RED-GREEN-REFACTOR. T1 locks down requirements as executable tests. T2 is the creative task (largest). T3 is the integration gate before story review.

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| Skill file too long for Claude context | Medium | Keep concise; deep methodology stays in knowledge file |
| Eduardo's voice difficult to validate programmatically | Low | Test for marker words/sections, not tone quality |
| CLI copy already tested elsewhere, redundant test | Low | Check existing tests first; skip if covered |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10 min | | |
| T2 | 20 min | | |
| T3 | 10 min | | |
