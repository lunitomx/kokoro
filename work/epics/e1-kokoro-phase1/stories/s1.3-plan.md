# S1.3 Plan: Knowledge Files

**Size:** S (3 tasks)
**Date:** 2026-03-03
**Design:** s1.3-design.md

## Tasks

### T1: Update CLI to overwrite kokoro-* files on re-run (S)

**TDD Cycle:**
- RED: Test that `kokoro init` re-run overwrites `kokoro-*.md` files but preserves user files
- GREEN: Modify `_copy_dir_no_overwrite` to overwrite files with `kokoro-` prefix
- REFACTOR: Clean up if needed

**Files:**
- `tests/test_cli.py` — add `TestKnowledgeFiles` class
- `src/kokoro/cli.py` — modify `_copy_dir_no_overwrite`

**Verification:** `uv run pytest && uv run ruff check . && uv run pyright`

**AC refs:** story.md scenarios 2, 3

### T2: Create knowledge content files (M)

**TDD Cycle:**
- RED: Test that each knowledge file exists and has minimum content (>50 lines, framework sections)
- GREEN: Create 5 knowledge files from `conocimientoraiz/` source material
- REFACTOR: Remove `.gitkeep`

**Files:**
- `extension/.claude/knowledge/kokoro-metodologia.md` — create
- `extension/.claude/knowledge/kokoro-phase1-diagnostico.md` — create
- `extension/.claude/knowledge/kokoro-phase1-vision.md` — create
- `extension/.claude/knowledge/kokoro-phase1-poda.md` — create
- `extension/.claude/knowledge/kokoro-phase1-finanzas.md` — create
- `extension/.claude/knowledge/.gitkeep` — delete
- `tests/test_knowledge.py` — create (content validation tests)

**Verification:** `uv run pytest && uv run ruff check . && uv run pyright`

**AC refs:** story.md scenario 1

### T3: Integration test (XS)

**Manual verification:**
- `kokoro init --target /tmp/kokoro-test` → knowledge files appear
- Re-run → kokoro files updated
- Add user file → re-run preserves it
- Verify all tests GREEN

**Verification:** `uv run pytest -v`

**Dependencies:** T1 → T2 → T3

## Execution Order

1. **T1** — CLI logic first (risk-first: if prefix approach doesn't work, pivot early)
2. **T2** — Content creation (bulk of work, depends on T1 for overwrite behavior)
3. **T3** — Integration validation

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| Knowledge files too large for Claude context | Medium | Keep each file focused, <200 lines |
| Content duplication with CLAUDE.md | Low | Knowledge = frameworks/process, CLAUDE.md = voice/identity |

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1 | 10 min | | |
| T2 | 20 min | | |
| T3 | 5 min | | |
