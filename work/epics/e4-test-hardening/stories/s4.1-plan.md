# S4.1 Plan — Conftest Consolidation

> **Story:** S4.1 | **Tasks:** 2

## T1: Migrate 10 test files to conftest fixtures

For each of the 10 test files:

1. Remove `EXTENSION_DIR = ...` and `SKILL_PATH = ...` module-level constants
2. Update `content(self)` fixtures to accept `commands_path: Path` and read from it
3. Update `test_skill_file_exists` to accept `commands_path: Path`
4. Update CLI test classes (`test_copied_skill_matches_source`, etc.) to use `commands_path`
5. Run `uv run pytest tests/test_{name}.py` after each file
6. Run `uv run pyright` and `uv run ruff check` after all files

**Commit:** `refactor(s4.1): T1 — migrate 10 test files to conftest fixtures`

## T2: Verify and clean up

1. Run full test suite: `uv run pytest` — 263/263 must pass
2. Verify: `grep -r "EXTENSION_DIR" tests/` returns zero matches
3. Verify: `grep -r "SKILL_PATH" tests/` returns zero matches
4. Run `uv run pyright` and `uv run ruff check` — clean

**Commit:** only if cleanup needed
