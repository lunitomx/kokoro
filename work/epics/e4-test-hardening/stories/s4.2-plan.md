# S4.2 Plan — Anti-Vocabulary Gate

> **Story:** S4.2 | **Tasks:** 2

## T1: RED — Add anti-vocabulary helper + tests to all 10 skill test files

1. Add `find_prohibited_words()` function to `tests/conftest.py`
   - Takes content string, returns list of (word, line_num, line_text) tuples
   - Prohibited words: cliente, producto, precio, gratis, descuento
   - Exclusions: blockquote lines (`>`), substitution instructions (`no "`),
     quoted references (`"word"`), risk categories (`riesgo de`),
     methodology names (`arbol de productos`), financial categories
     (`productos fisicos`)
2. Add `test_no_prohibited_vocabulary` to Content test class in all 10 files:
   - test_forces.py (TestForcesContent)
   - test_canvas.py (TestCanvasContent)
   - test_interviews.py (TestInterviewsContent)
   - test_validate.py (TestValidateContent)
   - test_diagnose.py (TestEduardoVoice)
   - test_finance.py (TestEduardoVoice)
   - test_mountain.py (TestEduardoVoice)
   - test_prune.py (TestEduardoVoice)
   - test_kokoro_router.py (TestEduardoVoice)
   - test_kokoro_session.py (TestEduardoVoice)
3. Run tests — expect RED (violations in finance, mountain, forces)
4. Run pyright + ruff — must be clean

**Commit:** `feat(s4.2): T1 — RED anti-vocabulary tests for all 10 skill files`

## T2: GREEN — Fix skill files that have violations

Fix prohibited words in skill files (replace with Eduardo vocabulary):

1. kokoro-finance.md: "cliente/clientes" -> "invitado/invitados" (lines 49-50, 84-85)
2. kokoro-mountain.md: "clientes" -> "invitados", "producto" -> "creacion", "precio" -> "inversion" (lines 39, 41, 143)
3. kokoro-forces.md: "descuento" -> "condiciones especiales" (line 198)
4. Run all tests — expect GREEN (273 pass)
5. Run pyright + ruff — must be clean

**Commit:** `feat(s4.2): T2 — GREEN fix prohibited vocabulary in skill files`
